import { ref, set } from "firebase/database";
import { realtimeDB } from "../../services/firebaseConfig";

export async function updateDeliveryLocation(deliveryId, lat, lng) {
  await set(ref(realtimeDB, 'deliveries/' + deliveryId), {
    lat,
    lng,
    timestamp: Date.now()
  });
}
